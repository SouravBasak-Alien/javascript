let nam = `ney`;
if(nam.length > 4){
     nam = `pele`; 
     
}
console.log(nam);

let player = `messi`;
player = `pogba`;
player = `silva`;
console.log(player)

const country = `Bangladesh`;
console.log(country)